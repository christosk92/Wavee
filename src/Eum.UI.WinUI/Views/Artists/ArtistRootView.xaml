<!--  Copyright (c) Microsoft Corporation and Contributors.  -->
<!--  Licensed under the MIT License.  -->

<UserControl
    x:Class="Eum.UI.WinUI.Views.Artists.ArtistRootView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:artists="using:Eum.UI.ViewModels.Artists"
    xmlns:controls="using:Eum.UI.WinUI.Controls"
    xmlns:controls1="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.UI.Converters"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Eum.UI.WinUI.Views.Artists"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xamlConverters="using:Eum.UI.WinUI.XamlConverters"
    mc:Ignorable="d">
    <UserControl.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition />
        </TransitionCollection>
    </UserControl.Transitions>
    <UserControl.Resources>


        <xamlConverters:TemplateTypeTolayoutConverter x:Key="TemplateTypeToItemTemplateConverter">
            <xamlConverters:TemplateTypeTolayoutConverter.GridLayout>
                <DataTemplate x:DataType="artists:DiscographyViewModel">
                    <Grid Height="200" Background="Red" />
                </DataTemplate>
            </xamlConverters:TemplateTypeTolayoutConverter.GridLayout>
            <xamlConverters:TemplateTypeTolayoutConverter.HorizontalStack>
                <DataTemplate x:DataType="artists:DiscographyViewModel">
                    <Grid Height="200" Background="Red" />
                </DataTemplate>
            </xamlConverters:TemplateTypeTolayoutConverter.HorizontalStack>
        </xamlConverters:TemplateTypeTolayoutConverter>
        <Style x:Key="GridViewImage" TargetType="Image">
            <Setter Property="Stretch" Value="UniformToFill" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel Padding="56,44,56,16">
            <Grid>
                <controls:ImageTransitionControl
                    Height="450"
                    Margin="-56,-44,-56,-16"
                    Source="{x:Bind ViewModel.Artist.Header, Mode=OneWay}" />
                <StackPanel
                    Padding="0,10,0,24"
                    VerticalAlignment="Bottom"
                    Orientation="Vertical"
                    Spacing="8">
                    <TextBlock
                        Margin="0,0,0,-12"
                        FontWeight="ExtraBold"
                        Opacity=".6"
                        Text="Artist" />
                    <TextBlock
                        x:Name="PlaylistNameBig"
                        FontSize="56"
                        FontWeight="Bold"
                        Text="{x:Bind ViewModel.Artist.Name, Mode=OneWay}" />
                    <TextBlock
                        x:Name="DescriptionBig"
                        Margin="0,0,0,8"
                        FontSize="18"
                        FontWeight="SemiBold"
                        MaxLines="2"
                        Opacity="0.8"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Text="1,357,781 monthly listeners"
                        TextTrimming="CharacterEllipsis" />

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Style="{ThemeResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE102;" />
                                <TextBlock Text="Play" />
                            </StackPanel>
                        </Button>
                        <Button>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE14B;" />
                                <TextBlock Text="Shuffle" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Discography, Mode=OneWay}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="artists:DiscographyGroup">
                        <controls1:DockPanel Margin="0,0,0,48" LastChildFill="True">
                            <Grid Margin="0,0,0,12" controls1:DockPanel.Dock="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="24"
                                    FontWeight="SemiBold"
                                    Text="{x:Bind Title}" />

                                <NavigationView
                                    Grid.Column="1"
                                    Margin="0,0,-230,0"
                                    HorizontalAlignment="Right"
                                    HorizontalContentAlignment="Right"
                                    IsBackButtonVisible="Collapsed"
                                    IsSettingsVisible="False"
                                    PaneDisplayMode="Top"
                                    Visibility="{x:Bind CanSwitchTemplates, Converter={StaticResource TrueToVisibleConverter}}">

                                    <interactivity:Interaction.Behaviors>
                                        <core:EventTriggerBehavior EventName="ItemInvoked">
                                            <core:EventTriggerBehavior.Actions>
                                                <core:InvokeCommandAction Command="{x:Bind SwitchTemplateCommand}" />
                                            </core:EventTriggerBehavior.Actions>
                                        </core:EventTriggerBehavior>
                                    </interactivity:Interaction.Behaviors>
                                    <NavigationView.MenuItems>
                                        <NavigationViewItem IsSelected="True" Tag="0">
                                            <NavigationViewItem.Icon>
                                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xF0E2;" />
                                            </NavigationViewItem.Icon>
                                        </NavigationViewItem>

                                        <NavigationViewItem Tag="1">
                                            <NavigationViewItem.Icon>
                                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE14C;" />
                                            </NavigationViewItem.Icon>
                                        </NavigationViewItem>
                                    </NavigationView.MenuItems>
                                </NavigationView>
                            </Grid>

                            <ItemsRepeater
                                VerticalAlignment="Top"
                                controls1:DockPanel.Dock="Top"
                                ItemsSource="{x:Bind Items}"
                                Layout="{x:Bind TemplateType, Converter={StaticResource TemplateTypeToLayoutConverter}, Mode=OneWay}"
                                VirtualizingStackPanel.VirtualizationMode="Recycling">
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="artists:DiscographyViewModel">
                                        <Border>
                                            <Border BorderThickness="0">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="4*" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                                    <Border>
                                                        <Border.Background>
                                                            <ImageBrush Stretch="UniformToFill">
                                                                <ImageBrush.ImageSource>
                                                                    <BitmapImage
                                                                        DecodePixelHeight="250"
                                                                        DecodePixelWidth="250"
                                                                        UriSource="{x:Bind Image}" />
                                                                </ImageBrush.ImageSource>
                                                            </ImageBrush>
                                                        </Border.Background>
                                                    </Border>
                                                    <StackPanel Grid.Row="1" Margin="0,4,0,0">
                                                        <TextBlock
                                                            HorizontalAlignment="Stretch"
                                                            FontSize="15"
                                                            FontWeight="SemiBold"
                                                            MaxLines="1"
                                                            Text="{x:Bind Title}"
                                                            TextWrapping="Wrap" />

                                                        <TextBlock
                                                            HorizontalAlignment="Stretch"
                                                            FontSize="13"
                                                            FontWeight="Normal"
                                                            MaxLines="1"
                                                            Text="{x:Bind Description}" />
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </Border>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </controls1:DockPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </StackPanel>
    </ScrollViewer>
</UserControl>
