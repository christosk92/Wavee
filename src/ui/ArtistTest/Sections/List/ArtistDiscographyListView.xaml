<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ArtistTest.Sections.List.ArtistDiscographyListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:list="using:ArtistTest.Sections.List"
    mc:Ignorable="d">

    <Grid>
        <ItemsRepeater ItemsSource="{x:Bind Items}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="local:ArtistDiscographyView">
                    <list:ArtistDiscographyLazyTracksView
                        Title="{x:Bind Title}" 
                        Image="{x:Bind Image}"
                        Tracks="{x:Bind Tracks}"
                        Id="{x:Bind Id}"
                        />
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </Grid>
</UserControl>
