<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Wavee.UI.WinUI.Views.Artist.Sections.List.ArtistDiscographyListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Wavee.UI.ViewModels"
    xmlns:list="using:Wavee.UI.WinUI.Views.Artist.Sections.List"
    xmlns:artist="using:Wavee.UI.ViewModels.Artist"
    mc:Ignorable="d">

    <Grid>
        <ItemsRepeater 
            x:Name="ArtistTracksRepeater"
            ItemsSource="{x:Bind Items}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="artist:ArtistDiscographyView">
                    <list:ArtistDiscographyLazyTracksView
                        Title="{x:Bind Title}" 
                        Image="{x:Bind Image}"
                        Tracks="{x:Bind Tracks}"
                        Id="{x:Bind Id}"
                        />
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </Grid>
</UserControl>
