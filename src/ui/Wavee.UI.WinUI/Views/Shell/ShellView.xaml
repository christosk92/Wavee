<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Wavee.UI.WinUI.Views.Shell.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sidebar="using:Wavee.UI.WinUI.Views.Sidebar"
             mc:Ignorable="d">

    <sidebar:SidebarControl SidebarItems="{x:Bind ViewModel.SidebarItems}"
                            Playlists="{x:Bind ViewModel.Playlists}"
                            User="{x:Bind ViewModel.CurrentUser, Mode=OneWay}"
                            x:Name="SidebarControl" />
</UserControl>
