<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Wavee.UI.WinUI.Views.Shell.Sidebar.GeneralSidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wavee.UI.WinUI.Views.Shell.Sidebar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <FontIcon Glyph="{x:Bind Item.Icon, Mode=OneWay}"
                  FontSize="16"
                  VerticalAlignment="Center"
                  Height="16" />

        <TextBlock Text="{x:Bind Item.Content, Mode=OneWay}"
                   Margin="10,0"
                   VerticalAlignment="Center"
                   Grid.Column="1" />
    </Grid>
</UserControl>
