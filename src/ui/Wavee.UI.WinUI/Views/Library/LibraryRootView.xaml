<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Wavee.UI.WinUI.Views.Library.LibraryRootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Wavee.UI.WinUI.Views.Library"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:labs="using:CommunityToolkit.Labs.WinUI"
             mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition  Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <labs:Segmented VerticalAlignment="Top"
                        IsItemClickEnabled="True"
                        ItemClick="TopNav_OnItemClick"
                        x:Name="TopNav">
            <labs:SegmentedItem Tag="songs"
                                Content="Songs" />
            <labs:SegmentedItem Tag="albums"
                                Content="Albums" />
            <labs:SegmentedItem Tag="artists"
                                Content="Artists" />
            <labs:SegmentedItem Content="Podcasts"
                                IsEnabled="False" />
        </labs:Segmented>

        <ContentControl x:Name="MainContent"
                        Grid.Row="1"
                        VerticalContentAlignment="Stretch"
                        HorizontalContentAlignment="Stretch" />
    </Grid>
</UserControl>
