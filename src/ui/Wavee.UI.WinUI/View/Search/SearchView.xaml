<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Wavee.UI.WinUI.View.Search.SearchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:local="using:Wavee.UI.WinUI.View.Search"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:search="using:Wavee.UI.ViewModel.Search"
    mc:Ignorable="d">

    <Grid Padding="48,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="18">
            <Run Text="Results for '" />
            <Run FontWeight="Bold" Text="{x:Bind ViewModel.SearchText, Mode=OneWay}" />
            <Run Text="'" />
        </TextBlock>
        <labs:Segmented
            Grid.Row="1"
            Margin="0,12"
            ItemsSource="{x:Bind ViewModel.Groups, Mode=OneWay}">
            <labs:Segmented.ItemTemplate>
                <DataTemplate x:DataType="search:SearchItemGroup">
                    <TextBlock Text="{x:Bind Title}" />
                </DataTemplate>
            </labs:Segmented.ItemTemplate>
        </labs:Segmented>

        <TextBlock
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="It's kinda empty here... Try another keyword"
            Visibility="{x:Bind ViewModel.HasResults, Converter={StaticResource TrueToCollapsedConverter}, Mode=OneWay}" />
        <ItemsRepeater Grid.Row="2" ItemsSource="{x:Bind ViewModel.Groups, Mode=OneWay}" />
    </Grid>
</UserControl>
