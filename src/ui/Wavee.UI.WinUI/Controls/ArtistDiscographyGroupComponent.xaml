<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Wavee.UI.WinUI.Controls.ArtistDiscographyGroupComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:controls1="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Wavee.UI.Features.Artist.ViewModels"
    DataContext="{x:Bind Item, Mode=OneWay}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock
            VerticalAlignment="Center"
            FontSize="16"
            FontWeight="SemiBold"
            Text="{x:Bind Item.Title, Mode=OneWay}" />

        <controls:Segmented Grid.Row="0" Grid.Column="1">
            <controls:Segmented.Items>
                <controls:SegmentedItem>
                    <controls:SegmentedItem.Icon>
                        <FontIcon FontFamily="/Assets/Fonts/MediaPlayerIcons.ttf#Media Player Fluent Icons" Glyph="&#xF0E2;" />
                    </controls:SegmentedItem.Icon>
                </controls:SegmentedItem>
                <controls:SegmentedItem>
                    <controls:SegmentedItem.Icon>
                        <FontIcon FontFamily="/Assets/Fonts/MediaPlayerIcons.ttf#Media Player Fluent Icons" Glyph="&#xEA37;" />
                    </controls:SegmentedItem.Icon>
                </controls:SegmentedItem>
            </controls:Segmented.Items>
        </controls:Segmented>

        <ItemsRepeater
            x:Name="ItemsViewData"
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="0,12,0,0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            ItemsSource="{Binding Items, Mode=OneWay}">
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="viewModels:ArtistViewDiscographyItemViewModel">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <controls1:ConstrainedBox Grid.Row="0" AspectRatio="1:1">
                            <Image Source="{x:Bind MediumImageUrl, Mode=OneWay}" />
                        </controls1:ConstrainedBox>

                        <StackPanel
                            Grid.Row="1"
                            Height="60"
                            Margin="0,6,0,0"
                            Orientation="Vertical">
                            <TextBlock
                                FontSize="15"
                                FontWeight="SemiBold"
                                MaxLines="2"
                                Text="{x:Bind Name, Mode=OneWay}"
                                TextWrapping="Wrap" />
                            <TextBlock
                                FontSize="13"
                                Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"
                                Text="{x:Bind Year, Mode=OneWay}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
            <ItemsRepeater.Layout>
                <UniformGridLayout
                    ItemsJustification="SpaceBetween"
                    ItemsStretch="None"
                    MaximumRowsOrColumns="200"
                    MinColumnSpacing="6"
                    MinItemHeight="270"
                    MinItemWidth="200"
                    MinRowSpacing="12"
                    Orientation="Horizontal" />
            </ItemsRepeater.Layout>
        </ItemsRepeater>
    </Grid>
</UserControl>
