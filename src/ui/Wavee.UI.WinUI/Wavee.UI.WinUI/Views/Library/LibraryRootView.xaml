<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="Wavee.UI.WinUI.Views.Library.LibraryRootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
             xmlns:library="using:Wavee.UI.ViewModels.Library"
             xmlns:listViewBase="using:Wavee.UI.WinUI.Behaviors.ListViewBase"
             xmlns:local="using:Wavee.UI.WinUI.Views.Library"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
             mc:Ignorable="d">
    <UserControl.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition />
        </TransitionCollection>
    </UserControl.Transitions>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Margin="12,8,12,8"
              Padding="4"
              Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
              BorderBrush="{ThemeResource ControlStrokeColorOnAccentDefaultBrush}"
              BorderThickness="1"
              CornerRadius="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <toolkit:Segmented IsItemClickEnabled="True"
                               ItemsSource="{x:Bind ViewModel.RootNavigationItems}"
                               Style="{StaticResource PivotSegmentedStyle}">
                <interactivity:Interaction.Behaviors>
                    <listViewBase:ItemClickedBehavior ItemClickedCommand="{x:Bind ViewModel.RootNavigationClickedCommand}" />
                </interactivity:Interaction.Behaviors>
                <toolkit:Segmented.ItemTemplate>
                    <DataTemplate x:DataType="library:ILibraryComponentViewModel">
                        <Grid x:Name="ContentHolder"
                              ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="PART_IconPresenter"
                                              Content="{x:Bind Converter={StaticResource ViewModelToIconConverter}}" />

                            <TextBlock x:Name="PART_ContentPresenter"
                                       Grid.Column="1"
                                       VerticalAlignment="Center"
                                       Text="{x:Bind Converter={StaticResource ViewModelToTextConverter}}" />
                        </Grid>
                    </DataTemplate>
                </toolkit:Segmented.ItemTemplate>
            </toolkit:Segmented>

            <ContentPresenter Grid.Column="1"
                              HorizontalAlignment="Right"
                              HorizontalContentAlignment="Stretch"
                              Content="{x:Bind ComponentToCommands(ViewModel.CurrentComponent), Mode=OneWay}" />
        </Grid>

        <ContentPresenter x:Name="MainContent"
                          Grid.Row="1"
                          VerticalAlignment="Top"
                          HorizontalContentAlignment="Stretch"
                          VerticalContentAlignment="Top" />
    </Grid>
</UserControl>
