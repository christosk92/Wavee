<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Wavee.UI.WinUI.Components.Player.PlayingOnRemoteDeviceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Wavee.UI.WinUI.Components.Player"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemAccentColorLight2}"
          Margin="1,0"
          Padding="0,0,0,4"
          CornerRadius="0,0,8,8">
        <StackPanel Margin="8,0"
                    Orientation="Horizontal">
            <TextBlock>
                <Run Text="Playing on" FontSize="14" FontWeight="Normal" /> <Run 
                                                   FontWeight="Bold" FontSize="14"
                                               Text="{x:Bind Device.DeviceName,Mode=OneWay}" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
